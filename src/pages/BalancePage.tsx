import { Box } from "@mui/material";
import React from "react";

const BalancePage = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        margin: 20,
      }}
    >
      <h2>Balance</h2>
    </Box>
  );
};

export default BalancePage;
