import { Box } from "@mui/material";
import React from "react";

const ExpensesPage = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        margin: 20,
      }}
    >
      <h2>Расходы</h2>
    </Box>
  );
};

export default ExpensesPage;
